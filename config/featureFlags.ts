// This flag controls whether the app uses local sample data or attempts to connect to external services.
// Set to `true` for local development, testing, and Vercel deployment without environment variables.
// Set to `false` when you have configured Supabase and Cloudinary environments.
export const useSampleData = true;
